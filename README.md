# 百炼

横版关卡制 ARPG。**城镇整备 → 闯关打怪 → 打 Boss → 掉装备 → 打造强化 → 变强 → 推进主线。**

| | |
|---|---|
| 引擎 | Godot **4.7.x** |
| 平台 | PC（Windows） |
| 当前阶段 | **M1 · 战斗原型**（第 0–4 周） |
| 完整计划 | [docs/game-development-plan.md](docs/game-development-plan.md) |

---

## 怎么跑

1. 用 Godot 4.7.x 打开本目录下的 `project.godot`
2. 按 `F5`。主场景是 `scenes/stages/test_room.tscn`

**操作**：`A`/`D` 或 `←`/`→` 移动　·　`Space`/`W`/`↑` 跳跃　·　`J` 攻击（未实现）　·　`K` 闪避（未实现）

---

## 目录结构

```
res://
├── scenes/          场景（.tscn）
│   ├── characters/  角色
│   ├── enemies/     敌人
│   ├── stages/      关卡与测试房间
│   └── ui/          界面
├── scripts/         脚本（.gd）
│   ├── core/        核心服务：状态机、存档、事件总线、RNG
│   ├── components/  可复用组件：Hitbox / Hurtbox / Health / Skill
│   └── characters/  角色逻辑
├── data/            数据资源（.tres）← 内容都长在这里，脚本里不写魔法数字
│   ├── characters/  enemies/  equipment/  skills/  stages/
├── assets/          sprites / audio / fonts
├── addons/          第三方插件
└── docs/            设计与剧情文档
```

---

## 当前技术约定

- **视口 640×360**，`canvas_items` 拉伸到 1280×720 窗口（整数倍，像素不糊）
- **渲染后端 GL Compatibility**：2D 项目够用、启动快、兼容性好。切换方式：项目设置 → 渲染 → 渲染方法
- **贴图过滤 Nearest**：为将来的像素美术准备
- **碰撞层**（项目设置 → 层名称已命名）：

  | 层 | 位值 | 用途 |
  |---|---|---|
  | 1 | 1 | player |
  | 2 | 2 | enemy |
  | 3 | 4 | world（地形） |
  | 4 | 8 | player_hitbox |
  | 5 | 16 | enemy_hitbox |

- **角色数值全部走 `@export`**：在检查器里改，改完立刻按 `F5` 看效果，不要改代码重跑

---

## 开发日志

### 2026-09-13
- 项目初始化：Godot 4.7.2 项目骨架、输入映射、目录结构、碰撞层命名
- 玩家角色：水平移动（带加速度/摩擦力）、跳跃（带**土狼时间**与**跳跃缓冲**）、可变下落重力
- 测试房间 `test_room.tscn`：地面 + 两块平台，用于验证跳跃手感
- 设计文档落地：`docs/game-development-plan.md`
- **已知待办**：中文字体尚未接入，场景内文字暂用英文。Godot 默认主题字体不含 CJK 字形，
  接对话框系统（M3）之前必须先接入思源黑体等 OFL 授权字体，否则中文会显示成方框

---

## 协作约定

| 谁 | 负责什么 |
|---|---|
| AI（神荼） | 架构设计、角色状态机、存档序列化、网络同步、Code Review、卡点救援 |
| 神 | 按模块实现（配讲解）、数值调优、关卡摆放、剧情文本、测试、决策 |

原则：**凡是能自己啃下来的模块，都自己写。** 目标是会做游戏，不是当产品经理。

---

## 里程碑

- [ ] **M1** 战斗原型（第 0–4 周）← 在这里
- [ ] **M2** 单机垂直切片（第 5–9 周）
- [ ] **M3** 第一章成型（第 10–14 周）
- [ ] **M4** 打磨与扩容（第 15–20 周）
- [ ] **M5** 联机（第 21 周起，需 M4 达标才解锁）
