# Godot 4+ 专属
.godot/
/android/

# Godot 导入缓存
.import/

# 导出配置（含本机路径，不入库）
export.cfg
export_presets.cfg

# 自动生成的翻译资源
*.translation

# C# / Mono（本项目暂未使用，先留着）
.mono/
data_*/
mono_crash.*.json

# 导出的构建产物
/build/

# 编辑器与系统垃圾
.vscode/
.idea/
*.tmp
Thumbs.db
desktop.ini

# 显卡驱动在项目目录里写的日志（NVIDIA 会在 cwd 下建目录）
/NVIDIA Corporation/

# Godot 无头运行（--headless）时的 user:// 回退目录，正常用编辑器不会产生
/Godot/

# 设计/计划/过程文档一律不进入本仓库（见 docs/adr/0001，文档在仓库外维护）
/docs/
